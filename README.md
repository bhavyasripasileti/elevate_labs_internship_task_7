# elevate_labs_internship_task_7
A simple Python task that connects to a SQLite database, runs SQL queries to summarize sales data, prints the results, and generates a basic bar chart using Matplotlib.
